/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost", "127.0.0.1", "pen-tutor.com", "pentutor.onrender.com", "pen-tutor-api.onrender.com"],
  },
};

export default nextConfig;
